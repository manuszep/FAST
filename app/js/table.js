var FAST = FAST || {};

/**
 *  FAST.Table
 *
 *  @depends on
 *      - jQuery
 */

(function(F, $, undefined) {
    /**
     *  Private area
     */


    /**
     *  Public area
     */
    var init = function() {
        var $tables = $('table');
    };

    /**
     *  Module declarations
     */
    F.Table = {
        init: init
    };
})(FAST, jQuery);
